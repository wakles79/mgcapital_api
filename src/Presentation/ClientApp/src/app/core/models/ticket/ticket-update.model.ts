import { TicketBaseModel } from '@app/core/models/ticket/ticket-base.model';

export class TicketUpdateModel extends TicketBaseModel {

  constructor(ticket: TicketUpdateModel) {
    super(ticket);
  }
}
