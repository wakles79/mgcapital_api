import { TimeAgoExtendsPipe } from './time-ago-extends.pipe';

describe('TimeAgoExtendsPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeAgoExtendsPipe();
    expect(pipe).toBeTruthy();
  });
});
